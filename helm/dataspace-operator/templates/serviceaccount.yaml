{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dataspace-operator.serviceAccountName" . }}
  labels:
    {{- include "dataspace-operator.labels" . | nindent 4 }}
{{- end }}
